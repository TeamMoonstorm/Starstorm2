namespace EntityStates.Emotes
{
    /*public class RestEmote : BaseEmote
    {
        public override void OnEnter()
        {
            animString = "Rest";
            animDuration = 8f;
            normalizeModel = true;

            CustomEffectComponent effectComponent = GetComponent<CustomEffectComponent>();
            if (effectComponent)
            {
                if (effectComponent.hasSheath && effectComponent.chargeAttackEffect == Assets.nemChargedSlashStartFXBlue)
                {
                    animString = "SitChair";
                    animDuration = 0.6f;
                }
            }

            base.OnEnter();
        }
    }*/
}